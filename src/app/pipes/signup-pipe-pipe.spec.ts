import { SignupPipePipe } from './signup-pipe-pipe';

describe('SignupPipePipe', () => {
  it('create an instance', () => {
    const pipe = new SignupPipePipe();
    expect(pipe).toBeTruthy();
  });
});
